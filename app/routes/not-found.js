import Route from '@ember/routing/route';

/**
 * Ruta para páginas no encontradas (404)
 */
export default class NotFoundRoute extends Route {
  // Esta ruta captura cualquier URL que no coincida con las rutas definidas
}
